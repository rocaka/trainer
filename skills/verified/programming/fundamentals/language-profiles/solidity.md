---
id: programming.fundamentals.solidity
language: Solidity
runtime: EVM
maps:
  programming.values-and-labels:
    example: 'string memory name = "小明";'
    introduceNow: [label, text-value]
    defer: [string-type, memory-data-location, semicolon]
---

# Solidity foundation profile

Begin with the learner's Solidity context, but do not pretend its extra syntax is already understood. Visibly mark `string memory` as “the language-specific description of this information; we will unpack it after basic labels and values.” Only after the foundational concept is secure should the lesson expand type, data location, contract storage, transactions, or EVM execution.
