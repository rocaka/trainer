---
id: programming.fundamentals.javascript-typescript
language: JavaScript/TypeScript
maps:
  programming.values-and-labels:
    example: 'let name = "小明"'
    introduceNow: [label, text-value]
    defer: [type-inference, mutability]
---

# JavaScript / TypeScript foundation profile

Use source from the active project when possible. Start with `let name = "小明"`; explain `let` as “create a label” before using the word declaration. Do not introduce TypeScript type annotations until the learner can distinguish a label from its value.
